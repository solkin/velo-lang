# Boolean extension functions

ext(bool b) str() str {
    if b then "true" else "false"
};

ext(bool b) int() int {
    if b then 1 else 0
};

ext(bool b) not() bool {
    if b then false else true
};


