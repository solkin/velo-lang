native class Sys(int pid) {

    native func print(str text) str;

    native func input() str;

    func println(str text) str {
        print(text.con("\n"));
    };

};

class[Sys] sys = Sys(4);
sys.println("Hello, World!");
sys.print(sys.input());
