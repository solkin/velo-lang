var fib = func(num) {
    var a = 0;
    var b = 1;
    var tmp = 0;
    var i = 2;
    while (i <= num) {
        tmp = a + b;
        a = b;
        b = tmp;
        i = i + 1;
    };
    b;
};

println("Fib: " + fib(93));