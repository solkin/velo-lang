include "lang/terminal.vel";

any fib = func (int num) int {
    int a = 0;
    int b = 1;
    int tmp = 0;
    int i = 2;
    while (i <= num) {
        tmp = a + b;
        a = b;
        b = tmp;
        i += 1;
    };
    b;
};

term.print("Fib: ");
term.println(fib(93).str);