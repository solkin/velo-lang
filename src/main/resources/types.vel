str s = "s";
bool b = true;
int i = 1;
#byte c = 2;
float f = 3.0;
slice[slice[int]] l = sliceOf[slice[int]]();
fn[void] n = func() void {};
# Pair parsing is supported, compilation is not
# pair[int,int] p = pairOf[int,int](1, 1);
