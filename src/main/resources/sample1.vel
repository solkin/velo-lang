a = list(10, {2+2}, "hello").ext(
    lambda copy() {
        it
    }
);

# a[2] = "2";
z = lambda(t, y) {
    19 + 2 + t - y;
};

b = len(a);
println(b);
println(len("hello"));
println("hello".len);
println("hello".hash);
println("hello".sub(1, 3));

println(a.len);
println(z(5+1, 6));

if (a[2] == "hello") {
    println(a[1]);
    println(a.sub(1, a.len)[1]);
};

m = a.copy().map(
    lambda(t) {
        "'" + t + "'";
    }
);
println(m[0]);
