#var a = 2;
#a = ((a + 2) * 6 + 6) / 3;
#func() {
#}();
#var b = func() {
#};
#b();

#if 1 < 2 then println(1) else println(2);

#func test(n) {
#    if n < 2 then 9 else n;
#};
#println(test(1));

#println(a);

func fib(n) {
    if n < 2 then n else fib(n - 1) + fib(n - 2);
};

println(fib(15));

#var n = if false then 5 else 6;
#println(n);