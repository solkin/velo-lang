var l = list(10, 20, 30, 40, 50);

var r = l.map(func(index, item) {
    var a = item - 25;
    a > 0;
});
var i = 0;
while (i < l.size) {
    print(l[i]);
    print("-");
    println(r[i]);
    i = i + 1;
};
println("");