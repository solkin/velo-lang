func traverse(index, col) {
    print(col);
    print(" ");
    false
};

var printWorld = func(world) {
    world.map(
        func(index, row) {
            var res = row.map(traverse);
            println(" ");
            res
        }
    )
};

var w = list(
    list(0, 1, 0, 0, 0, 0),
    list(0, 0, 1, 0, 0, 0),
    list(1, 1, 1, 0, 0, 0),
    list(0, 0, 0, 0, 0, 0),
    list(0, 0, 0, 0, 0, 0),
    list(0, 0, 0, 0, 0, 0)
);
var r = printWorld(w);
