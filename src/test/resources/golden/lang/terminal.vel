# Terminal is a host (native) class registered on the runtime; its type is
# synthesized from the registration — no declaration needed. This module
# just provides the conventional global instance.
Terminal term = new Terminal();
